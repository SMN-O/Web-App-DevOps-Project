# azure-pipelines.yml

trigger:
- main

pool:
  vmImage: 'ubuntu-latest'
  parrallel: 1


steps:
  - task: Docker@2
    inputs:
      containerRegistry: 'agitated_banach'
      command: 'buildAndPush'
      repository: 'smnostuff/devopsimage'
      Dockerfile: '**/dockerfile'