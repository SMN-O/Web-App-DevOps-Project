# azure-pipelines.yml

trigger:
- main

pool:
  vmImage: 'ubuntu-latest'
  parrallel:1


steps:
  - task: Docker@2
    inputs:
      containerRegistry: 'DevOps Docker'
      command: 'buildAndPush'
      repository: 'smnostuff/devopsimage'
      tags: 'latest'
      Dockerfile: '**/dockerfile'